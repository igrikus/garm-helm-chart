## -- GARM application configuration
garm:
  ## -- Enable the GARM web UI.
  ui:
    enabled: true

  ## -- GARM admin user credentials. These will be stored in a Kubernetes secret.
  admin:
    ## -- The name of the secret containing the admin credentials.
    ## -- If not set, a secret will be created with a generated password.
    secretName: ""
    ## -- The default admin username to use when generating the secret.
    defaultUsername: "admin"
    ## -- The default admin email address to use when generating the secret.
    defaultEmail: "admin@example.com"
    ## -- The key in the secret for the admin username.
    usernameKey: "username"
    ## -- The key in the secret for the admin password.
    passwordKey: "password"
    ## -- The key in the secret for the admin email.
    emailKey: "email"
    ## -- The key in the secret for the JWT secret.
    jwtSecretKey: "jwt-secret"
    ## -- The key in the secret for the database passphrase.
    dbPassphraseKey: "db-passphrase"

  ## -- The base URL for the GARM service. This can be used for the Ingress and for GARM's own configuration.
  url: "https://garm.example.com"
  ## -- Specify callback URL (with correct URI path). If empty, garm.url will be used.
  callbackUrl: ""
  ## -- Specify metadata URL (with correct URI path). If empty, garm.url will be used.
  metadataUrl: ""
  ## -- Specify webhook URL (with correct URI path). If empty, garm.url will be used.
  webhookUrl: ""

  ## -- TLS configuration for the GARM API server.
  ## -- This is optional. A reverse proxy or Ingress with TLS termination can be used instead.
  ## -- If enabled, you must provide a secret containing the TLS certificate and key
  ## -- and mount it using extraVolumes and extraVolumeMounts.
  tls:
    enabled: false
    ## -- In-container path to the TLS certificate file.
    ## -- NOTE: If your certificate is signed by an intermediary CA, this file
    ## -- must contain the entire certificate bundle.
    ## -- See: https://github.com/cloudbase/garm/blob/main/doc/config.md#the-api-server-config-section
    certPath: ""
    ## -- In-container path to the TLS key file.
    keyPath: ""

## -- Image configuration for GARM and its components.
image:
  ## -- GARM image repository.
  repository: "ghcr.io/cloudbase/garm"
  ## -- GARM image pull policy.
  pullPolicy: "Always"
  ## -- GARM image tag. Overrides the chart's appVersion.
  tag: ""

## -- Overrides for the chart name.
nameOverride: ""
## -- Overrides for the full chart name.
fullnameOverride: ""

## -- ServiceAccount configuration.
serviceAccount:
  ## -- Specifies whether a service account should be created.
  create: true
  ## -- Automatically mount a ServiceAccount's API credentials.
  automount: true
  ## -- Annotations to add to the service account.
  annotations: {}
  ## -- The name of the service account to use.
  ## -- If not set and create is true, a name is generated using the fullname template.
  name: ""

## -- Annotations for both the server and operator Pods.
podAnnotations: {}
## -- Labels for both the server and operator Pods.
podLabels: {}

## -- Service configuration.
service:
  ## -- Kubernetes service type.
  type: ClusterIP
  ## -- The port for the GARM service.
  port: 9997
  annotations: {}

## -- Deployment strategy type.
strategyType: "RollingUpdate"

## -- Ingress configuration.
ingress:
  ## -- Enable or disable the Ingress resource.
  enabled: false
  ## -- The public-facing host for the Ingress resource. Required if ingress.enabled is true.
  host: ""
  ## -- IngressClassName for the Ingress resource.
  className: ""
  ## -- Annotations for the Ingress resource.
  annotations: {}
  ## -- TLS configuration for the Ingress.
  tls:
    ## -- The name of the secret containing the TLS certificate.
    secretName: ""

## -- Resource requests and limits for the containers.
resources:
  ## -- Resources for the main GARM container.
  garm:
    limits:
      cpu: 200m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 512Mi
  ## -- Resources for the substitute-config init container.
  substituteConfig:
    limits:
      cpu: 50m
      memory: 64Mi
    requests:
      cpu: 25m
      memory: 32Mi
  ## -- Resources for the config-init init container.
  configInit:
    limits:
      cpu: 100m
      memory: 256Mi
    requests:
      cpu: 50m
      memory: 128Mi
  ## -- Resources for the GARM operator manager.
  operator:
    limits:
      cpu: 500m
      memory: 128Mi
    requests:
      cpu: 128m
      memory: 64Mi

## -- GARM operator manager configuration.
operator:
  enabled: true
  image:
    repository: "ghcr.io/igrikus/garm-operator"
    pullPolicy: "IfNotPresent"
    tag: ""
  replicas: 1
  leaderElection: true
  ## -- Override the GARM API URL used by the operator.
  ## -- If unset, the chart uses the internal Service URL with the scheme selected from garm.tls.enabled.
  # garmBaseUrl: "https://garm.example.com"
  serviceAccount:
    create: true
    automount: true
    annotations: {}
    name: ""
  podAnnotations: {}
  podLabels: {}
  securityContext:
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault
  containerSecurityContext:
    readOnlyRootFilesystem: true
    allowPrivilegeEscalation: false
    capabilities:
      drop:
        - ALL

## -- Additional volumes to mount in the Pod.
extraVolumes: []
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

## -- Additional volume mounts for the GARM container.
extraVolumeMounts: []
# - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

## -- Persistence configuration for the GARM database.
persistence:
  ## -- Enable or disable persistence.
  enabled: true
  ## -- Annotations for the PVC.
  annotations:
    helm.sh/resource-policy: keep
  ## -- The storage class to use for the PVC.
  storageClassName: ""
  ## -- The access modes for the PVC.
  accessModes:
    - ReadWriteOnce
  ## -- The size of the PVC.
  size: 1Gi

## -- RunnerTemplate CRs that pools can reference by name.
runnerTemplates: []
  # - name: "linux-github"
  #   description: "Custom Linux GitHub runner bootstrap"
  #   osType: "linux"
  #   forgeType: "github"
  #   data: |
  #     #!/bin/bash
  #     echo "install runner"

## -- Configuration for different git forges.
forges:
  ## -- Gitea server configurations.
  gitea:
    ## -- GiteaEndpoint CRs.
    endpoints: []
      #   ## -- Item names become Kubernetes resource names and must be lowercase RFC 1123 subdomains.
      # - name: "my-gitea"
      #   baseURL: "https://gitea.example.com"
      #   apiBaseURL: "https://gitea.example.com/api/v1"
      #   description: "Primary Gitea"
      #   caCertBundleSecretRef:
      #     name: "gitea-ca"
      #     key: "ca.crt"

    ## -- GiteaCredentials CRs.
    credentials: []
      #   ## -- Item names become Kubernetes resource names and must be lowercase RFC 1123 subdomains.
      # - name: "my-gitea"
      #   endpointName: "my-gitea"
      #   description: "Gitea PAT"
      #   patSecretRef:
      #     name: "gitea-token"
      #     key: "token"

    ## -- GiteaOrganization CRs.
    organizations: []
      #   ## -- Item names become Kubernetes resource names and must be lowercase RFC 1123 subdomains.
      # - name: "my-org"
      #   endpointName: "my-gitea"
      #   credentialsName: "my-gitea"
      #   organizationName: "MyGiteaOrg"
      #   poolBalancerType: "roundrobin"
      #   ## If webhookSecretRef is omitted, the operator generates a random secret
      #   ## and passes it once to GARM during creation. GARM stores it encrypted.
      #   ## installWebhook defaults to true only when webhookSecretRef is omitted.
      #   installWebhook: true
      #   webhookInsecureSSL: false
      #   webhookSecretRef:
      #     name: "gitea-webhook"
      #     key: "secret"

    ## -- Gitea Repository CRs.
    repositories: []
      #   ## -- Item names become Kubernetes resource names and must be lowercase RFC 1123 subdomains.
      # - name: "my-repo"
      #   endpointName: "my-gitea"
      #   credentialsName: "my-gitea"
      #   owner: "MyGiteaOrg"
      #   repositoryName: "my-repo"
      #   poolBalancerType: "roundrobin"
      #   ## If webhookSecretRef is omitted, the operator generates a random secret
      #   ## and passes it once to GARM during creation. GARM stores it encrypted.
      #   ## installWebhook defaults to true only when webhookSecretRef is omitted.
      #   installWebhook: true
      #   webhookInsecureSSL: false
      #   webhookSecretRef:
      #     name: "gitea-webhook"
      #     key: "secret"

    ## -- Org- or repository-scoped Gitea Pool CRs. pools[].name is required and becomes spec.runnerPrefix.
    pools: []
      #   ## -- Item names become Kubernetes resource names and must be lowercase RFC 1123 subdomains.
      # - name: "my-pool"
      #   ## -- Kubernetes GiteaOrganization resource name, not the literal forge organization name.
      #   organizationName: "my-org"
      #   repositoryName: ""
      #   endpointName: "my-gitea"
      #   providerName: "gcp"
      #   image: "ubuntu-24.04"
      #   flavor: "medium"
      #   osType: "linux"
      #   osArch: "amd64"
      #   minIdleRunners: 0
      #   maxRunners: 5
      #   enabled: true
      #   runnerBootstrapTimeoutMinutes: 20
      #   runnerInstallTemplateName: "linux-gitea"
      #   priority: 0
      #   tags:
      #     - "my-garm-runner"
      #   extraSpec:
      #     custom_labels:
      #       my-custom-label: "my-value"
      #     disable_updates: true

  ## -- GitHub server configurations.
  github:
    endpoints: []
      #   ## -- Item names become Kubernetes resource names and must be lowercase RFC 1123 subdomains.
      # - name: "github"
      #   baseURL: "https://github.com"
      #   apiBaseURL: ""
      #   uploadBaseURL: ""
      #   description: "Public GitHub"
      #   caCertBundleSecretRef:
      #     name: "github-ca"
      #     key: "ca.crt"
    credentials: []
      #   ## -- Item names become Kubernetes resource names and must be lowercase RFC 1123 subdomains.
      # - name: "github-pat"
      #   endpointName: "github"
      #   description: "GitHub PAT"
      #   authType: "pat"
      #   patSecretRef:
      #     name: "github-token"
      #     key: "token"
      # - name: "github-app"
      #   endpointName: "github"
      #   authType: "app"
      #   appAuth:
      #     appID: 123
      #     installationID: 456
      #     privateKeySecretRef:
      #       name: "github-app-key"
      #       key: "private-key.pem"
    repositories: []
      #   ## -- Item names become Kubernetes resource names and must be lowercase RFC 1123 subdomains.
      # - name: "github-repo"
      #   endpointName: "github"
      #   credentialsName: "github-pat"
      #   owner: "my-org"
      #   repositoryName: "my-repo"
      #   poolBalancerType: "roundrobin"
      #   ## If webhookSecretRef is omitted, the operator generates a random secret
      #   ## and passes it once to GARM during creation. GARM stores it encrypted.
      #   ## installWebhook defaults to true only when webhookSecretRef is omitted.
      #   installWebhook: true
      #   webhookInsecureSSL: false
      #   webhookSecretRef:
      #     name: "github-webhook"
      #     key: "secret"
    enterprises: []
      #   ## -- Item names become Kubernetes resource names and must be lowercase RFC 1123 subdomains.
      # - name: "github-enterprise"
      #   endpointName: "github"
      #   credentialsName: "github-app"
      #   enterpriseName: "my-enterprise"
      #   poolBalancerType: "roundrobin"
      #   webhookSecretRef:
      #     name: "github-webhook"
      #     key: "secret"
    pools: []
      #   ## -- Item names become Kubernetes resource names and must be lowercase RFC 1123 subdomains.
      # - name: "github-repo-pool"
      #   repositoryName: "github-repo"
      #   endpointName: "github"
      #   providerName: "gcp"
      #   image: "ubuntu-24.04"
      #   flavor: "medium"
      #   runnerInstallTemplateName: "linux-github"

## -- Configuration for different providers.
providers: []
  # - name: "gcp"
  #   type: "external"
  #   description: "GCP provider"
  #   executable: "/opt/garm/providers.d/garm-provider-gcp"
  #
  #   ## -- Dynamic configuration for the provider's .toml file.
  #   ## -- You can add any keys here, and they will be added to the ConfigMap.
  #   config:
  #     project_id: "my-project"
  #     zone: "us-central1-a"
  #     network_id: "projects/my-project/global/networks/default"
  #     subnetwork_id: "projects/my-project/regions/us-central1/subnetworks/default"
  #     external_ip_access: true

## -- Array of extra objects to deploy with the release
## -- It can be in "string" or "object" format
extraManifests: []
