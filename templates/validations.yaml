{{- if .Values.runnerTemplates }}
{{- fail "runnerTemplates are not supported by the Phase 5 CRD-based chart; RunnerTemplate CR support is deferred" }}
{{- end }}
{{- range $pool := .Values.forges.gitea.pools }}
{{- if $pool.runnerInstallTemplate }}
{{- fail (printf "Gitea pool %q uses runnerInstallTemplate, which is not supported by the Phase 5 chart" ($pool.name | default "<missing-name>")) }}
{{- end }}
{{- end }}
{{- range $pool := .Values.forges.github.pools }}
{{- if $pool.runnerInstallTemplate }}
{{- fail (printf "GitHub pool %q uses runnerInstallTemplate, which is not supported by the Phase 5 chart" ($pool.name | default "<missing-name>")) }}
{{- end }}
{{- end }}
